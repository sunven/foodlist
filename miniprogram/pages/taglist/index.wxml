<!--pages/taglist/index.wxml-->
<view class="container">
  <view>
    <input class="weui-input" model:value="{{name}}" placeholder="请输入标签名" />
    <button type="primary" bindtap="onSave">新增</button>
  </view>
</view>
<view class="container">
  <view wx:for="{{tagList}}" wx:key="_id">{{item.name}}</view>
</view>